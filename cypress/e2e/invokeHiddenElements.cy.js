//This example show how you can use the invoke command to change the state in the dom

/*
describe('This is and example, this test will fail', () => {
    cy.cy.visit('url');
    cy.contains('Dynamic Content')
   .should('be.hidden').invoke('show')
   cy.contains('Dynamic Content').should('be.visible')
})
*/