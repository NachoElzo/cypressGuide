//This example show how you can use the invoke command to change the state in the dom

/*
describe('This is and example, this test will fail', () => {
    cy.cy.visit('url');
    cy.contains('Dynamic Content')
    //will show and click over a hidden element
   .should('be.hidden').invoke('show').click
   cy.contains('Dynamic Content').should('be.visible')
})

//events 
cy.get('element')
.trigger('mouseover')

//after trigger the event windwo etc, we validate that is visible
 cy.get('hoveredElement')
.should('be.visible')
//////////////////////////////////////////////
cy.get('element')
.trigger(mouseout)

cy.get('hoveredElement')
.should('not.be.visible')
*/